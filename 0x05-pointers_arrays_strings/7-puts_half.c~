#include "main.h"
/**
 *
 *
 */
void puts_half(char *str)
{
	int i = 0;
	int j;

	while (str[i] != '\0')
		i++;
	j = i / 2;

	if (i % 2 == 1)
		j++;
	while (str[j] != '\0')
	{
		_putchar(str[j]);
		j++;
	}
}
