#include "main.h"
/**
 *_strlen - prints the length of a string
 *@s; is a character parammeter
 *Return: returns the length
 */
int _strlen(char *s)
{
	int i = 0;
	while (*s != '\0')
	{
		i++;
		*s++;
	}
	return (0);
