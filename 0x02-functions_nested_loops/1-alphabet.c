#include "main.h"

/**
 *here is the code to print alphabet in lower case
 *print_alphabet: prints in lower case
 *Description - prints alphabet with lower case
 *
 */

void print_alphabet(void)
{
	char i;
	
	for (i = 'a'; i <= 'z'; i++)
		_putchar(i);
	_putchar('\n');
}
