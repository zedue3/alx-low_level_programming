#include "main.h"
/**
 * add - computes the two numbers
 * Return: returns the result
 * @i: one parameter
 * @j: second parameter
 */
int add(int i, int j)
{
	int result;

	result = i + j;
	return (result);
}
