#include "main.h"
/**
 *mul - compute the multiplication of two integers
 *@a: one int parameter
 *@b: other int parameter
 *Return: returns the result of the multiplication
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
